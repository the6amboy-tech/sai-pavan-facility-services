"use client"; import { useState } from "react"; import { ArrowUpRight, Check, MapPin, Phone } from "lucide-react"; import PageShell from "@/components/page-shell"; import { maps, phone, services } from "@/lib/site-data";
export default function Contact(){const [done,setDone]=useState(false);const [loading,setLoading]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);const f=new FormData(e.currentTarget);const r=await fetch('/api/contact',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(f))});setLoading(false);if(r.ok){setDone(true);e.currentTarget.reset()}}return <PageShell><main><section className="sub-hero contact-hero"><div className="shell"><p className="kicker">START A CONVERSATION</p><h1>Let’s make your<br/>facility <em>work better.</em></h1><p>Tell us what you need. Our team will shape a professional solution around your property and requirements.</p></div></section><section className="contact-page shell"><div className="contact-side"><p className="kicker">GET IN TOUCH</p><h2>One partner for<br/><em>better facilities.</em></h2><p>Our team is ready to understand your requirement and recommend the right facility-management or maintenance support.</p><a href={`tel:${phone}`}><Phone/> +91 {phone}</a><address><MapPin/> Plot No. 201, Guruharan Residency<br/>Near Ramanujan Circle, Beside Bliss Hotel<br/>Tirupati, Andhra Pradesh, India</address><a className="directions" href={maps} target="_blank"><MapPin/> Get Directions</a><ul><li><Check/> Customized facility solutions</li><li><Check/> Responsive maintenance support</li><li><Check/> One accountable point of contact</li></ul></div><form onSubmit={submit} className="quote-form"><label>Full Name *<input name="name" required placeholder="Your full name"/></label><label>Phone Number *<input name="phone" required placeholder="Your phone number" type="tel"/></label><label>Service Required *<select name="service" required defaultValue=""><option value="" disabled>Select a service</option>{services.map(s=><option key={s.slug}>{s.title}</option>)}</select></label><label>Location / City *<input name="location" required placeholder="Your location or city"/></label><label>Requirement / Message *<textarea name="message" required placeholder="Tell us about your requirement"/></label><label>Email Address <input name="email" placeholder="Optional" type="email"/></label><button className="button" disabled={loading}>{loading?'Sending...':'Request a callback'}<ArrowUpRight size={18}/></button>{done&&<p className="form-success">Thank you — our team will contact you shortly.</p>}</form></section></main></PageShell>}
