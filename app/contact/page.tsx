"use client"; import { useState } from "react"; import { ArrowUpRight, Check, Phone } from "lucide-react"; import PageShell from "@/components/page-shell"; import { services } from "@/lib/site-data";
export default function Contact(){const [done,setDone]=useState(false);const [loading,setLoading]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);const f=new FormData(e.currentTarget);const r=await fetch('/api/contact',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(f))});setLoading(false);if(r.ok){setDone(true);e.currentTarget.reset()}}return <PageShell><main><section className="sub-hero contact-hero"><div className="shell"><p className="kicker">START A CONVERSATION</p><h1>Let’s make your<br/>facility <em>better.</em></h1><p>Tell us what you need. We’ll shape a service plan around your space and your standards.</p></div></section><section className="contact-page shell"><div className="contact-side"><p className="kicker">GET IN TOUCH</p><h2>A better tomorrow<br/>starts <em>today.</em></h2><p>Our team is ready to understand your facility and recommend the right support.</p><a href="tel:8500229978"><Phone/> +91 85002 29978</a><ul><li><Check/> Transparent, tailored quotes</li><li><Check/> Responsive local support</li><li><Check/> One accountable partner</li></ul></div><form onSubmit={submit} className="quote-form"><label>Name<input name="name" required placeholder="Your full name"/></label><label>Phone<input name="phone" required placeholder="Your phone number" type="tel"/></label><label>Email<input name="email" placeholder="Your email address" type="email"/></label><label>Service<select name="service" required defaultValue=""><option value="" disabled>Select a service</option>{services.map(s=><option key={s.slug}>{s.title}</option>)}</select></label><label>Tell us about your facility<textarea name="message" placeholder="Type of property, location, requirements..."/></label><button className="button" disabled={loading}>{loading?'Sending...':'Request a callback'}<ArrowUpRight size={18}/></button>{done&&<p className="form-success">Thank you — our team will contact you shortly.</p>}</form></section></main></PageShell>}
