import Link from "next/link"; import { notFound } from "next/navigation"; import { ArrowUpRight, Check, Mail, MessageCircle } from "lucide-react"; import PageShell from "@/components/page-shell"; import { email, services, whatsappUrl } from "@/lib/site-data";
export function generateStaticParams(){return services.map(s=>({slug:s.slug}))} export default async function ServiceDetail({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const service=services.find(s=>s.slug===slug);if(!service)return notFound(); const Icon=service.icon;const items=service.items||["Site-specific service plans","Trained, supervised teams","Quality checks and clear reporting","Flexible schedules and responsive support"];return <PageShell><main><section className="service-detail-hero"><div className="detail-image" style={{backgroundImage:`url(${service.image})`}}/><div className="shell"><p className="kicker light">{service.category}</p><Icon/><h1>{service.title}</h1><p>{service.description}</p><div className="detail-actions"><a href={whatsappUrl(service.title)} target="_blank" className="button"><MessageCircle size={17}/> Enquire on WhatsApp</a><a href={`mailto:${email}?subject=${encodeURIComponent(`Enquiry: ${service.title}`)}`} className="button ghost light-ghost"><Mail size={17}/> Email enquiry</a></div></div></section><section className="service-detail-content shell"><div><p className="kicker">DESIGNED AROUND YOU</p><h2>Reliable service.<br/><em>Professional support.</em></h2></div><div><p>Every Sai Pavan service begins with understanding your facility. We create a practical service plan, assign the appropriate team and remain accountable through clear communication and responsive support.</p><ul>{items.map(item=><li key={item}><Check/>{item}</li>)}</ul></div></section><section className="detail-next"><div className="shell"><span>Need a different solution?</span><Link href="/services">Explore all services <ArrowUpRight size={17}/></Link></div></section></main></PageShell>}
