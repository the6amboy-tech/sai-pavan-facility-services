import Link from "next/link"; import { notFound } from "next/navigation"; import { ArrowUpRight, Check } from "lucide-react"; import PageShell from "@/components/page-shell"; import { services } from "@/lib/site-data";
export function generateStaticParams(){return services.map(s=>({slug:s.slug}))} export default async function ServiceDetail({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const service=services.find(s=>s.slug===slug);if(!service)return notFound(); const Icon=service.icon;return <PageShell><main><section className="service-detail-hero"><div className="detail-image" style={{backgroundImage:`url(${service.image})`}}/><div className="shell"><p className="kicker light">{service.category}</p><Icon/><h1>{service.title}</h1><p>{service.description}</p><Link href="/contact" className="button">Request this service <ArrowUpRight size={18}/></Link></div></section><section className="service-detail-content shell"><div><p className="kicker">DESIGNED AROUND YOU</p><h2>Reliable service.<br/><em>Real care.</em></h2></div><div><p>Every Sai Pavan service begins with understanding your facility. We create a practical service plan, assign trained professionals and stay accountable through clear checks and responsive support.</p><ul><li><Check/> Site-specific service plans</li><li><Check/> Trained, supervised teams</li><li><Check/> Quality checks and clear reporting</li><li><Check/> Flexible schedules and responsive support</li></ul></div></section><section className="detail-next"><div className="shell"><span>Need a different solution?</span><Link href="/services">Explore all services <ArrowUpRight size={17}/></Link></div></section></main></PageShell>}
