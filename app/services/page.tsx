import Link from "next/link"; import { ArrowUpRight } from "lucide-react"; import PageShell from "@/components/page-shell"; import { services } from "@/lib/site-data";
export default function Services(){return <PageShell><main><section className="sub-hero services-hero"><div className="shell"><p className="kicker">WHAT WE DO</p><h1>One team.<br/><em>Every solution.</em></h1><p>18 practical, professional services designed to keep your facility at its best.</p></div></section><section className="catalog shell"><div className="catalog-intro"><p className="kicker">COMPLETE FACILITY SOLUTIONS</p><h2>Every detail,<br/><em>well looked after.</em></h2><p>Explore our services below. Each can be shaped around your site, schedule and service standards.</p></div><div className="catalog-grid">{services.map((s,i)=><Link href={`/services/${s.slug}`} key={s.slug} className="catalog-card"><div className="catalog-photo" style={{backgroundImage:`url(${s.image})`}}/><div className="catalog-info"><span>0{i+1}</span><s.icon/><h3>{s.title}</h3><p>{s.short}</p><b>Learn more <ArrowUpRight size={14}/></b></div></Link>)}</div></section></main></PageShell>}
