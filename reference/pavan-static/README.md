# Pavan static-site source drop

These files were supplied as the static-site version of the Pavan website and are kept here as a versioned reference. The deployed application is the Next.js implementation in `app/`, which now includes the enquiry flow and WhatsApp behaviour represented by these files.
